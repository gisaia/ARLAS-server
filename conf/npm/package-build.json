{
  "name": "arlas-api",
  "version": "0.0.0",
  "author": "gisaia",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/gisaia/ARLAS-server.git"
  },
  "scripts": {
    "build-release": "tsc -p tsconfig-build.json && tsc-alias",
    "postbuild": "rm -rf dist && mkdir dist && mv *.js.map *.js *.d.ts ./dist/"
  },
  "private": true,
  "dependencies": {
    "isomorphic-fetch": "3.0.0",
    "url": "0.11.0"
  },
  "devDependencies": {
    "tsc-alias": "^1.8.16",
    "typescript": "~5.9.3"
  },
  "overrides": {
    "isomorphic-fetch": {
      "node-fetch": "3.3.0"
    }
  }
}