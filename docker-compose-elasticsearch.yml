version: '3'

services:
  elasticsearch:
    #Update elasstic from 7.17.6 to 7.17.13 to fix elastict start on github actions runner in CI
    image: docker.elastic.co/elasticsearch/elasticsearch:${ELASTIC_VERSION:-7.17.13}
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - cluster.name=docker-cluster
      - node.name=docker-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - xpack.security.enabled=false
      - xpack.monitoring.enabled=false
      - xpack.graph.enabled=false
      - xpack.watcher.enabled=false
    volumes:
#      - /var/lib/elasticsearch:/usr/share/elasticsearch/data
      - ${ELASTIC_DATADIR}:/usr/share/elasticsearch/data
    ports:
      - 9200:9200