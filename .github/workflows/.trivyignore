# Status: Ignored for now - action to be done: upgrade keycloak
# Reason: need keycloak dependecy update to 26.0.6 
# Description: Potential Mitigations Requires access to the deployment's local network to exploit
CVE-2024-10039

# Status : Can be ignored
# Reason: ARLAS-server might create zip files but never consumes zip files  
# Description : Potential Mitigation from https://avd.aquasec.com/nvd/2023/cve-2023-45853/ : MiniZip in zlib through 1.3 has an integer overflow and resultant heap-based buffer overflow in zipOpenNewFileInZip4_64 via a long filename, comment, or extra field. [...]
CVE-2023-45853

# Status: Can be ignored
# Reason: ARLAS-server process has a RAM limitation
# Description: Potential Mitigations from https://avd.aquasec.com/nvd/2023/cve-2023-25193/ : Use resource-limiting settings provided by the operating system or environment
CVE-2023-25193

# Status: Can be ignored
# Reason: ARLAS-server uses Java which performs its own memory management
# Description: Potential Mitigations from https://avd.aquasec.com/nvd/2023/cve-2023-7272/ : [...] many languages that perform their own memory management, such as Java and Perl, are not subject to buffer overflows
CVE-2023-7272

# Status: Can be ignored
# Reason:  ARLAS-server uses Java which performs its own memory management
# Description : Potential Mitigations fromhttps://avd.aquasec.com/nvd/2024/cve-2024-13009/ : [...] languages such as Java, Ruby, and Lisp perform automatic garbage collection that releases memory for objects that have been deallocated
CVE-2024-13009

# Status: Can be ignored
# Reason: ARLAS-server uses Java which performs automatic bounds checking
# Description: Potential Mitigations from https://avd.aquasec.com/nvd/2026/cve-2026-0861/ : Choose a language or compiler that performs automatic bounds checking
CVE-2026-0861

# Status: Ignored for now - action to be done: upgrade docker image gcr.io/distroless/java17-debian12
# Reason: ARLAS-server does not expose any XML parsing functionality to untrusted sources.
# Description: In libexpat before 2.7.4, the doContent function does not properly determine the buffer size bufSize because there is no integer overflow check for tag buffer reallocation.  https://avd.aquasec.com/nvd/2026/cve-2026-25210/
CVE-2026-25210

# Status: Ignored for now - action to be done: upgrade Hazelcast : https://github.com/hazelcast/hazelcast/issues/26578
# Reason: need jackson-core dependecy update to 2.21.1
# Description: jackson-core: Number Length Constraint Bypass in Async Parser Leads to Potential DoS Condition
GHSA-72hv-8253-57qq

# Status: Ignored for now - action to be done: upgrade docker image gcr.io/distroless/java17-debian12
# Reason: ARLAS-server does not take PNG input.
# Description: https://access.redhat.com/security/cve/cve-2026-33416
CVE-2026-33416

# Status: Ignored for now - action to be done: upgrade docker image gcr.io/distroless/java17-debian12
# Reason: ARLAS-server does not take PNG input.
# Description: https://access.redhat.com/security/cve/cve-2026-33636
CVE-2026-33636
